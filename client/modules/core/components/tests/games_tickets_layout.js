const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import GamesTicketsLayout from '../games_tickets_layout';

describe('core.components.games_tickets_layout', () => {
  it('should do something');
});

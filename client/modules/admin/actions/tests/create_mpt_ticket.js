const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../create_mpt_ticket';

describe('admin.actions.create_mpt_ticket', () => {
  it('should do something');
});

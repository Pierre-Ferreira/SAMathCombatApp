const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../update_mpt_ticket';

describe('admin.actions.update_mpt_ticket', () => {
  it('should do something');
});

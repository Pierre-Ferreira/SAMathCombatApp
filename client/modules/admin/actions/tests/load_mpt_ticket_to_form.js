const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../load_mpt_ticket_to_form';

describe('admin.actions.load_mpt_ticket_to_form', () => {
  it('should do something');
});
